export interface AddLinkProps {
  isOpen: boolean;
  onClose?: () => void;
};

export default function AddLink({ isOpen , onClose}: AddLinkProps) {

    return (
        <div>
            
        </div>
    )
}