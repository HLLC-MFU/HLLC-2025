export type Localization = {
  th: string;
  en: string;
};

export type Photo = {
  coverPhoto: string;
  bannerPhoto: string;
  thumbnail: string;
  logoPhoto: string;
};

export type Location = {
    latitude: number;
    longitude: number;
};